let valor = prompt("digite um valor");
let segundo_valor = prompt("digite outro valor");
const vetor = [9, 0, 8, 1, 7, 190, 12, 87, -10, -200, 14, 11];
console.log(`o cubo do primeiro valor é:${Math.pow(valor, 3)} a raiz do segundo valor é:${Math.sqrt(segundo_valor)} e o maior elemento do vetor pre determinado é:${Math.max(...vetor)} e o menor elemento do vetor pre determinado é:${Math.min(...vetor)}`);
